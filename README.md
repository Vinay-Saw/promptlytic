# Promptlytics — Full project (Codespaces + CI)

This archive contains:
- Full `app/` implementation (FastAPI + LLM client + Playwright fetcher + executor)
- `.devcontainer/` for GitHub Codespaces
- GitHub Actions workflow for CI that installs Playwright and validates the app

See previous messages for setup instructions and security notes.
